@using ICDE.Lib.Dto.BeoordelingCriterea
@model List<BeoordelingCritereaDto>
@{
    ViewData["Title"] = "Bekijk BeoordelingCriterea";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>BeoordelingCritereas</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <h2> Maak criterea </h2>
        <form method="post" enctype="multipart/form-data" asp-action="MaakLes">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label for="Naam" class="control-label"></label>
                <input name="Naam" class="form-control" />
            </div>
            <div class="form-group">
                <label for="Beschrijving" class="control-label"></label>
                <input name="Beschrijving" class="form-control" />
            </div>
            <div class="form-group">
                <input type="submit" value="Maak" class="btn btn-primary" />
            </div>
        </form>
    </div>    
    <div class="col-md-4">
        <h2> Andere critereas </h2>
        @if (Model.Count > 0)
        {
            <table class="table">
                <thead>
                    <tr>
                        <th>Naam</th>
                        <th>Beschrijving</th>
                        <th>URL</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Naam</td>
                            <td>@item.Beschrijving</td>
                            <td><a href="/auteur/criterea/get/@item.GroupId">Bekijk</a></td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    </div>

</div>


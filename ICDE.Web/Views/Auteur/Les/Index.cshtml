@model ICDE.Web.Models.Lessen.LesIndexViewModel
@{
    ViewData["Title"] = "Bekijk les";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Lessen</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <h2> Maak les </h2>
        <form method="post" enctype="multipart/form-data" asp-action="MaakLes">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label for="Naam" class="control-label"></label>
                <input name="Naam" class="form-control" />
            </div>
            <div class="form-group">
                <label for="Beschrijving" class="control-label"></label>
                <input name="Beschrijving" class="form-control" />
            </div>
            <div class="form-group">
                <input type="submit" value="Maak" class="btn btn-primary" />
            </div>
        </form>
    </div>    
    <div class="col-md-4">
        <h2> Andere lessen </h2>
        @if (Model.Lessen.Count > 0)
        {
            <table class="table">
                <thead>
                    <tr>
                        <th>Naam</th>
                        <th>Beschrijving</th>
                        <th>VersieNummer</th>
                        <th>URL</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Lessen)
                    {
                        <tr>
                            <td>@item.Naam</td>
                            <td>@item.Beschrijving</td>
                            <td>@item.VersieNummer</td>
                            <td><a href="/les/get/@item.GroupId">Bekijk</a></td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    </div>

</div>


@model List<ICDE.Lib.Dto.Opdracht.OpdrachtDto>

@{
    ViewData["Title"] = "Maak opdracht";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Bekijk opdrachten</h1>

<h4>Bekijk opdrachten</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <table class="table">
            <thead>
                <tr>
                    <th>Naam</th>
                    <th>Beschrijving</th>
                    <th>Type</th>
                    <th>URL</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Naam</td>
                        <td>@item.Beschrijving</td>
                        <td>@item.Type</td>
                        <td><a href="/opdrachten/bekijk/@item.Id">Bekijk</a></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

